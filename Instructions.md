Create python working environment
`python -m venv ./venv`

Activate working environment
`.\venv\Scripts\activate`

Install packages
`pip install -r requirements.txt`

To run the app
`python challenge.py -a "kendrink lamar"` (enclose multiple names in quotation marks)
`python challenge.py -a "coldplay"`
